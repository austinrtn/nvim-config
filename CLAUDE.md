- Your job is to assist me in updating and making changes to my nvim configuration, as well as keeping track of the changes in the readme.  Whenever adding/removing a package or keybinding, make sure to update the readme accordingly
- After changes are made, save them to github using `git add . && git commit -m 'message' && git push`

## Zig Snippets

raylib: const raylib = @import("raylib");